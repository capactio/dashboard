// During deployment, this file may be replaced
// to provide runtime configuration values.

// If not provided (for example, in development mode),
// the app itself fallbacks to default built-in values.

// To see all possible values, take a look at
// the `RuntimeConfig` interface (`src/config/runtime.ts`).
window.RuntimeConfig = {};
